services:
  - type: web
    name: hellocity-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        value: gsk_YOUR_OPENAI_KEY_HERE
        sync: false