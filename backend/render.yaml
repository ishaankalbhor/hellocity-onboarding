services:
  - type: web
    name: hellocity-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: GROQ_API_KEY
        value: gsk_YOUR_ACTUAL_KEY_HERE
        sync: false